# Workspace Rules

- **Conventional Commit PR Names:** All pull requests and commits created by agents must follow the Conventional Commits format with a clear scope. For example: `<type>(<scope>): <description>` (e.g., `feat(auth): ...` or `fix(backend): ...`).
